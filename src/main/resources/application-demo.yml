chat:
  session:
    persistence-enabled: true
    persistence-path: ./data/chat-sessions-demo.json

spring:
  ai:
    mcp:
      client:
        enabled: false

milvus:
  enabled: false

knowledge:
  base:
    path: ./aiops-docs

prometheus:
  mock-enabled: true

cls:
  mock-enabled: true

agent:
  v2:
    max-verification-rounds: 2
    model-synthesis-enabled: false
